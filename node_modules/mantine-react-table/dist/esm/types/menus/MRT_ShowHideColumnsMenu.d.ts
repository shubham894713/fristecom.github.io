/// <reference types="react" />
import type { MRT_TableInstance } from '..';
interface Props<TData extends Record<string, any> = {}> {
    isSubMenu?: boolean;
    table: MRT_TableInstance<TData>;
}
export declare const MRT_ShowHideColumnsMenu: <TData extends Record<string, any> = {}>({ isSubMenu, table, }: Props<TData>) => JSX.Element;
export {};
